# 계측기 (Eval)

> **ARD 내부 소규모 프로젝트 #2** | 회장 명명 2026-07-01
> 폴더명은 영문(`Eval`), 프로젝트 정식명은 **계측기**.

## 무엇 (One-liner)
"하나의 에이전트가 어떤 작업을 **직접 하는 것**이 나은가, 아니면 **스킬을 명시적으로 호출**해서 하는 것이 나은가?" — 이 미결 질문을 **증거로** 답하기 위한 내부 연구 프로젝트.

## 왜 (배경)
- **계측기(Eval)** — 실행 계층(LLM + 하네스: 루프·컨텍스트·툴·검증)이 실제로 품질에 기여하는지를 A/B로 재는 도구. 하네스를 *정의*하는 문서가 아니라(그 정의는 ARCHITECTURE §2), 하네스가 값어치 있는지 *계측*한다.
- 헌법(CLAUDE.md)은 "코딩마다 ponytail, 디자인마다 ui-ux-pro-max, T4마다 speckit…" 식으로 **스킬 명시호출을 의무화**하고 있으나, 그 의무가 실제 품질을 올리는지는 **한 번도 측정된 적이 없었다**.
- 2026-07-01 실측: G2의 Skill 호출은 전체 통틀어 **2회**(1회는 테스트). 즉 의무는 산문으로만 존재하고 발화되지 않았다. → "걷어낼까 / 강제할까"를 정하려면 **직접 vs 스킬 품질 비교 데이터**가 먼저 필요.

## 연구 질문 (RQ)
- **RQ1.** 스킬 명시호출 결과물이 직접 수행 결과물보다 품질이 높은가? 조건은?
- **RQ2.** "지식형 스킬(도메인 사실·프레임)" vs "방법론형 스킬(체크리스트·프로세스)"의 부호가 다른가?
- **RQ3.** 어떤 스킬을 **라우팅(강제)**하고 어떤 스킬을 **opt-in**으로 둘 것인가? (헌법 개정 근거)

## 방법론 (재현 가능)
1. **생성자 고정, 변수는 스킬호출 여부 하나만.** 동일 에이전트 타입 2개 — 직접팔(스킬 금지) vs 스킬팔(해당 Skill 호출 후 수행). 동일 과제.
2. **블라인드 독립 심사.** 제3의 심사 에이전트(opus)가 두 결과물을 스킬흔적 제거·라벨 셔플 상태로 받아 루브릭 채점. 어느 쪽이 스킬인지 모름.
3. **패널.** 스킬당 심사관 3명 → 다수결 판정(2/3+).
4. **범주 표본.** 900개 전수(추정 180M~1B 토큰, 비현실적)가 아니라 **의무화·라우팅된 결정관련 스킬만** 표본.

## 산출물
- `results/` — 실험별 결과 리포트 (날짜_주제).
- `data/` — 원시 판정 데이터(JSON).
- `experiments/` — 실험 **사전등록(설계)** 문서. 실행 전 가설·분류·예측을 잠가 확증편향 차단.
- `eval_stats.py` — **통계 계측기(2026-07-14 신설).** point estimate 단독 산출을 코드가 금지한다.
  - MODE1 `--reanalyze`: judge 일치도 × |Δ|로 현 N=1 데이터 신뢰등급(HIGH/MEDIUM/LOW/NOISE) → `results/2026-07-14_statistical-reanalysis.md`
  - MODE2 `nrep_stats()`: variant당 N≥2 점수 → 평균·표준편차·95% 부트스트랩 CI·Cohen's d·유의판정 (3차 실험이 N≥3로 가면 진짜 유의성)
  - `--selftest`: 통계 정확성 수기 대조 9/9 PASS. 의존성 0(stdlib만).
- 이 프로젝트의 결론은 **헌법 개정 제안**의 근거가 된다(★반영은 회장 승인 후 — 이 폴더는 propose-only).

## 현황
- ✅ **1차 파일럿(n=1, 3작업)** + **2차 패널(12스킬, 36심사)** 완료 → `results/2026-07-01_skill-vs-direct.md`
- 📐 **3차 실험 설계 완료 · 실행 보류** → `experiments/2026-07-01_exp3-design_rigor-vs-scope.md` (회장 지시 2026-07-01: **설계만 해두고 실행 X**)
- ⏳ 미결: verification-before-completion·speckit 미검증 / 스킬당 1작업 한계 / "엄밀성 vs 범위팽창" 가설 미확정(3차 실행 대기)
- ⛔ **헌법엔 아직 아무것도 반영 안 함** (회장 지시: 결정 보류, 결과만 축적)

## 소유
- 상위: [ARD](../README.md) / 임원: CAR (ard-loop 소유자와 동일 계열)
- 관련: [[project-ard]], G2 CLAUDE.md 스킬 라우팅표
