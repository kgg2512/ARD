# 인사이트: {영상 제목}

- **출처:** {채널} — {url}
- **수확일:** {harvested_at} / **처리일:** {오늘}
- **영상 핵심 주제:** {한 줄}

## 1. 무엇을 가르치나 (이해)
{영상이 소개한 AI 에이전트 기법/오케스트라 패턴/스킬/MCP/워크플로우/1인 스타트업 셋업을 3~6줄로 요약. 자막 기반 사실만, 추측은 [추정] 라벨.}

## 2. G2 관련성
- **적용 가능 영역:** {예: 라우팅 / 검증 게이트 / 스킬 / MCP / 배포 자동화 / 특정 프로젝트}
- **신규성:** {이미 G2에 있는가? 있으면 '중복', 없으면 '신규'}
- **가치 (1~5):** {점수 + 한 줄 근거}

## 3. 구체적 적용 제안
{추상적 "좋다"가 아니라 실행 가능한 액션으로:}
- [ ] {예: `npx skills add <패키지>` — 출처 신뢰? 설치수?}
- [ ] {예: MCP `<x>` 추가 (settings.json) — 승인 필요}
- [ ] {예: CLAUDE.md 라우팅 표에 규칙 1줄}

## 4. 🔒 적용 판정 (3게이트 — 셋 다 통과해야 auto-apply)
- **① 주장 검증 (claim gate):** 콘텐츠의 핵심 메커니즘 주장을 G2 `[확인]`/`[추정]` 라벨로 판정. **과장·미검증 능력 주장은 출처가 아무리 신뢰돼도 auto-apply 불가**(→ 승인 큐). *출처 신뢰 ≠ 주장 참.*
  - 반례(실제): "유출 시스템 프롬프트를 싼 모델 지침에 붙이면 상위 모델급 결과" — 출처 GitHub 45k stars(=② 통과)지만 메커니즘 `[추정]` 거짓(프롬프트는 *행동 스타일*만 바꾸지 *가중치=능력*을 못 바꿈) → **① 실패로 기각.**
- **② 출처 게이트 (supply-chain):** **owner allowlist(anthropics·modelcontextprotocol·microsoft 등)만이 신뢰 근거.** ⚠️ **GitHub 스타수는 신뢰 신호가 아니다**(파밍 가능 — 실측 2026-07-09: 무명 레포 `affaan-m/ECC`가 GitHub상 22.7만 스타). 스타는 관심도/relevance일 뿐 — allowlist 밖이면 스타가 아무리 높아도 NO→승인 큐. (harvester가 `owner_trusted`·`star_suspect` 플래그를 부착함.)
- **③ 베이스라인 게이트 (plain Opus를 이기나?):** 이 지식/스킬/에이전트를 적용한 결과가 **"plain Opus + 좋은 컨텍스트" 단일 호출을 실제로 이기나?** 못 이기면(=Opus가 이미 함 / 조직도 미학용 / claude-flow 잔재) **기각.** *G2 자체 데이터: 위임 +285~515% 토큰·순차추론 품질 −39~70%·강제 스킬 다수 무승부·음수. 그러므로 기본값 = 단일모델, 추가는 "이길 때만". 자기발전 = 축적이 아니라 이기는 것만 남기는 가지치기.*
- **위험:** {공급망/보안/비용/아키텍처 영향}
- **결정:** {auto-applied / 회장 승인 대기 / 기각}

## 5. 적용 결과 (VERIFY — 적용했다면)
- **실행:** {설치 명령 + 결과(EXIT/설치됨 여부)}
- **검증:** {실제 호출/연결 확인. 미검증이면 '추적 중'}

## 6. 원장 반영
- `applied.json`에 기록: {type, name, source, applied_at, decision}
